language: node_js

node_js:
 - "0.10"
 - "0.12"
 - "iojs"

notifications:
  irc:
    channels:
      - "irc.mozilla.org#fxa"
    use_notice: false
    skip_join: false

before_install:
  - npm config set spin false

before_script:
  - npm i grunt-cli -g
  - npm run outdated

script:
  - npm test

matrix:
  allow_failures:
    - node_js: "0.12"
    - node_js: "iojs"
  fast_finish: true
